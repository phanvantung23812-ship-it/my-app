


// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("POSTGRES_PRISMA_URL") // 👈 这里的名字改了，用 Vercel 默认给的连接池地址
  directUrl = env("POSTGRES_URL_NON_POOLING") // 👈 新增这一行，用于数据库迁移时的直连地址
}

// 👇 这是我们定义的表结构
model Todo {
  //id        String   @id @default(cuid())  // 自动生成的唯一 ID
  id        Int      @id @default(autoincrement())
  content   String                           // 任务内容
  completed Boolean  @default(false)         // 是否完成，默认 false
  createdAt DateTime @default(now())         // 创建时间
}

